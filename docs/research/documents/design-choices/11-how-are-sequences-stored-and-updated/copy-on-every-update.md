# Copy on every update

Every change makes a new copy. Simple and obviously pure; the cost is proportional to the size for arrays, or logarithmic for chunk trees.

* Languages: the default for arrays in most pure settings; Erlang and Elixir tuples.
