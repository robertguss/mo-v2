# Static checking

The program is refused unless it passes the checker. There is no way around it. Catches mismatched kinds of values; cannot catch wrong logic.

* Languages: Go, Rust, Haskell, Swift.
