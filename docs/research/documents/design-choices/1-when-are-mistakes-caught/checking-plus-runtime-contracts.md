# Checking plus runtime contracts

Static checking, plus promises such as amount <= balance checked while the program runs. Catches wrong logic, but only when a run hits it.

* Languages: Eiffel, Ada, Racket.
