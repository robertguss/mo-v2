# Elixir and Erlang

Share: the look (D33), the BEAM runtime, let-it-crash with supervisors (D29), per-process memory (pools, D32). Lack: static checking (Elixir is adding a type system), effect tracking, proofs.
