# Gleam

Runs on the BEAM. Shares: statically checked, immutable, no null, errors as values, no OOP, plain data and modules. Lacks: effect tracking, capabilities, proofs, counting holders (uses BEAM garbage collection). Brace syntax.
