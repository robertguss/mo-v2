# Unlimited size by default

Numbers grow as large as needed and never overflow. Simplest to reason about. Slower: the program checks on every operation whether the number still fits in a machine word (1.68× on Experiment 3's benchmark 4).

* Languages: Koka, Python, Lean, Ruby, Haskell (Integer).
